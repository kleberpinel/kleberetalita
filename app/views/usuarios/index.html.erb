<h1>Listing users</h1>

<table width="300px">
  <tr>
    <th>Nome</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @users.each do |user| %>
  <tr>
    <td><%= user.nome %></td>
    <td><%= link_to 'Show', usuario_path(user) %></td>
    <td><%= link_to 'Edit', edit_usuario_path(user) %></td>
    <td><%= link_to 'Destroy', usuario_path(user), :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Post', new_usuario_path %>
