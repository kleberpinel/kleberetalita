<%= form_for( @user, :html => {:class=> "comment-form"}) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br />
    <%= f.text_field :nome, :value => @user.nome %>
  </div>

  <div class="field">
    <%= f.label :email %><br />
    <%= f.text_field :email, :value => @user.email %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>